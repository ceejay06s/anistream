services:
  - type: web
    name: anistream-backend
    runtime: node
    buildCommand: yarn && yarn build
    startCommand: yarn start
    plan: free
    healthCheckPath: /api
    envVars:
      - key: NODE_ENV
        value: production
